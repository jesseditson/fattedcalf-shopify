.wrapper {
  max-width: 100%; }

.site-header {
  position: absolute;
  top: 0px;
  left: 0px;
  right: 0px;
  z-index: 100;
  height: 80px; }

.nav-bar {
  background-color: rgba(255, 255, 255, 0.6);
  height: 100%; }
  .nav-bar .site-nav {
    padding-top: 0.5em;
    display: flex;
    justify-content: center; }
    .nav-bar .site-nav .header-logo-wrapper {
      display: flex;
      justify-content: center; }
    .nav-bar .site-nav li {
      width: 200px; }
    .nav-bar .site-nav a {
      padding: 0px; }
